{
  "name": "opacus-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Opacus Smart Contract Suite (DACRegistry, AgentRegistry, DataStream, MsgEscrow, OpacusCore)",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network ogTestnet",
    "deploy:0g": "hardhat run scripts/deploy.ts --network ogMainnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network ogMainnet",
    "verify": "VERIFY=true hardhat run scripts/deploy.ts",
    "integration": "hardhat run scripts/test-integration.ts --network hardhat"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.1.3",
    "@types/node": "^20.11.30",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
